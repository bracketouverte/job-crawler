{
  "name": "job-scrapper-crawler",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "crawl": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "crawl": "node dist/cli.js",
    "test": "npm run build && node --test \"dist/*.test.js\" \"dist/providers/*.test.js\""
  },
  "dependencies": {
    "better-sqlite3": "^12.9.0",
    "fast-xml-parser": "^4.5.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=22"
  }
}
